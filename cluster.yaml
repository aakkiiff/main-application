apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: cluster01
  region: ap-south-1
nodeGroups:
  - name: nodegroup1
    instanceType: t3.large
    desiredCapacity: 2
    volumeSize: 20
    ssh:
      allow: false
